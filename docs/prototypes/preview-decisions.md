# Preview decisions

Recorded September 13–14, 2026. Decisions describe intended behavior, not a claim that implementation or verification is complete. Current per-task status lives in `../migration/remaining-work.md`; active execution details live in local `CURRENT.md`. Later decisions supersede historical checkpoints below.

| Decision | Reason / constraint |
|---|---|
| Finish existing-feature public preview before A2A, terminal, streaming, or remote push expansion. | Reliable everyday chats and cohesive navigation are the release priority. Preserve isolated feature candidates; do not market unverified integrations. |
| Keep Bots, Sessions, and Activity. | Bots represent server profiles; Sessions contain conversation history; Activity contains actual scheduled tasks. |
| Keep a centered bird with its name underneath in chat. | Explicit user requirement. Identity must not be displaced by trailing actions. |
| Use the approved swept-wing bird, with basic coordinated colors and no enclosing circle. | Preserve existing deterministic profile buckets; customization, accessories, and animation remain deferred. |
| Enlarge the header bird to roughly 48 points with a glass name capsule and no dropdown chevron. | Match the latest reference proportions; keep profile switching available in controls and preserve accessible hit targets. |
| Use a soft palette-matched fade behind the floating chat header and status bar. | Real screenshots showed transcript text competing with controls. Preserve scroll-under and avoid a hard-edged panel, extra layout space, or intercepted touches. |
| Present “Thinking” with the latest meaningful activity preview; actions likewise preview their latest activity. | Expand to formatted history. Hide only verified Hermes spinner-status fragments in presentation, preserving raw content and code. Active-only subtle motion respects Reduce Motion. |
| Explicit Back wins over a late startup restore for the current view lifetime. | Preserve the remembered chat for cold launch, but do not undo an intentional dismissal during refresh. |
| Blank-transcript acceptance requires mixed short/long chat navigation, tab changes, prolonged time in another app, and cold reopen with messages visible before interaction. | Still open after September 14 phone feedback. Check running and completed responses and preserve reading position; screenshots after scrolling or accessibility-only observations do not establish resolution. |
| Sessions rows show title/time above a two-line latest-message preview. | Today uses local clock time, yesterday uses “Yesterday,” older messages a compact date. Prefer actual message timestamps and avoid per-row transcript downloads. |
| Prioritize blank-on-send/return and scroll-to-bottom reliability before remaining preview polish. | A down-arrow tap during deceleration must take effect and smoothly reach the bottom; Reduce Motion may use an immediate move. Verify actual motion and populated transcript before any compensating scroll. |
| Remove the generic floating “Hermes is working” pill. | Existing activity rows and Stop expose the running state; retain meaningful connection/recovery/error feedback. Avoid changing transcript insets simply because a running chat scrolls away from the bottom. |
| Use compact, consistent Thinking/action/task disclosures with no stale active Thinking after completion. | Preserve meaningful historical reasoning and task contents, but do not render empty status artifacts or old heavy task cards. Verify rich activity sequences, not just ACK responses. |
| Show a persistent Copy control for the latest completed assistant reply; older replies retain Copy in their context menu. | Avoid adding a separate 44-point footer under every historical reply in long chats, while preserving accessible targets and historical actions. |
| Updated first-run order: Welcome → Connect → optional Personalize. | Supersedes the earlier Appearance-before-Connect decision below. Failed first-login retries remain first-run; saved-server reauthentication bypasses personalization. |
| Header bird is slightly larger/lower, with the glass nameplate overlapping its bottom. | Keep centered placement, no dropdown, and existing details action; verify actual rendering before acceptance. |
| Use an outline of the approved bird for the Bots tab. | Supersedes the earlier filled-silhouette decision; keep the same recognizable shape. |
| Commit accepted batches locally on the integration branch; leave main and remote publishing untouched until separately approved. | Preserve previous local commits and allow Jacob to collaborate through an explicitly shared development branch, without promoting unfinished changes. |
| Distinguish stale Live Activity presentation from true background completion. | A last-known label must not claim completion. Closed-app terminal delivery needs authenticated ActivityKit push infrastructure and team/device validation; that deployment decision remains open. |
| Use sliders for model/reasoning/context; ellipsis for Files, workspace, and conditional secondary actions. | Frequent configuration is one tap away without a crowded header. Preserve existing guards and destinations. |
| Open a single controls sheet with direct model choices and supported discrete reasoning levels. | Remove the current-model → All models → second-sheet sequence. Preview slider movement locally; submit on release. |
| Show context only from authoritative usage data; fetch optional snapshots without delaying chat readiness. | Cumulative token totals are not context occupancy. Missing data remains explicitly unavailable, and stale results cannot overwrite newer state. |
| Use a separate plus and bottom-aligned multiline text/mic/send composer. | Keep one-line alignment unchanged. Preserve attachments, dictation, multiline input, and stop behavior. |
| Treat cron-origin conversations as scheduled history, not active jobs. | History provenance does not prove that a schedule still exists. Keep Hermes's deletion protections. |
| Hide empty Projects and consolidate Sessions filters while retaining organizer actions. | Reduce idle UI clutter without removing capabilities. Project and workspace path remain distinct concepts. |
| Show pinned conversations as an unlabelled top bird/name strip in the unfiltered Sessions view. | Matches the latest reference without an extra section heading. Keep same-bot conversations distinct, avoid duplicate ordinary rows, and retain pinned matches in filtered/search results. |
| Make bot-row taps start a chat; provide a separate read-only details action. | Avoid ambiguous tap behavior or implying a general profile editor exists. Server-default/profile creation remain in their real Settings flows. |
| Use consistent Settings access and remove circular Tools/Settings routes. | Keep current capabilities reachable through predictable navigation. |
| Use Kadu navigation and density as references, not as a template for copying assets or unsupported features. | Retain Semreh's cream/charcoal palette and original bird identity. Compare actual screens and motion, not source code alone. |
| Scope send animation to a newly submitted local message. | Restoring, paging, or reconciling history must not replay it or move the reader. Respect Reduce Motion. |
| Default implementation to Luna Max; use Sol Low for verification/escalation and Astra Low for visual design. | Control usage while retaining independent judgment for acceptance. |
| Serialize native verification on one simulator; root reviews substantial UI and integration. | Avoid shared-resource contention. The verifier must judge evidence, not only capture it. Each verdict applies to its tested source. |
| Keep working through bounded repairs; stop for actual blockers rather than routine design questions. | User authorized reasonable decisions with a log, not unbounded feature expansion or release deployment. |

Future A2A testing may use the user's WSL machine with a temporary, isolated Hermes instance. No instance has been started; exact host, isolation, lifetime, and cleanup must be resolved before using it.

September 14 phone-feedback decisions: use a minimal native loading indicator instead of nested message skeletons; preserve visible content during refresh. Reuse the bird artwork's alpha silhouette for the Bots tab. Keep native tab roots stable and limit content reveal to subtle opacity, never animate the entire navigation state. Settings prioritizes connection/chat/appearance controls over a decorative local profile card. Appearance uses one native title, cream/charcoal/System modes and coordinated accent presets; legacy stored themes remain compatible. Sheet edges and Tasks loading fill the themed canvas. Paging preserves the visible anchor and prefetches only near the loaded boundary, without dragging the reader to the bottom.

Verification checkpoint: signed Simulator core lifecycle/paging/send-stop gate v9 and Settings/theme cold-persistence gate v13 passed; integrator reviewed screenshots and sampled motion. Genuine older-page prepend measured zero anchor displacement in the bounded 126-row fixture. Coherent unit v2 passed 2,154 tests with 14 skips and zero failures; the preceding runner-startup stall is retained. Evidence lives in the local semreh-slice1-evidence directory. These results do not establish physical-iPhone frame rate, exhaustive intermittent-bug elimination, or large-Markdown workload performance.

September 14 onboarding follow-up: use Welcome → Appearance → Connect for first run; saved-server sign-in skips introductory pages. Reuse the real System/Light/Dark and accent preferences so customization is not a decorative preview. Use existing winged-S and bird assets, a single canvas and restrained motion. The new seven-second reference shows welcome/customization only; connection design follows our supported contract, not an assumed reference flow. QR remains a secret-free address import with explicit review and separate authentication; manual HTTPS entry is always available. Simulator verification covers scanner fallback and parser rules, not physical-camera recognition. Bot grouping must be backed by a real supported model; a visual grouping must never be presented as multi-agent collaboration.

Group capability audit: the pinned Hermes backend implements real hosted rooms through `groups.*`, distinct from A2A. Semreh currently has no room client or conversation UI; its Teams screen is a local mock and organizer groups are Projects. Keep the production Bots list as Your Team while the real integration is separately implemented and verified. The intended future order is Groups above Your Team, using actual gateway rooms, not synthetic local membership. Capability probing itself may initialize room-link state, so it is not treated as a side-effect-free discovery call. A group directory alone would not establish working collaborative chat.

Loading follow-up: preserve already-loaded bots and the resolved Activity profile while refreshing, and clear cancelled loading state on departure. Required task data becomes visible independently of optional delivery-target discovery; cancellation and generation checks still fence late responses. A settled screenshot is not proof against a transient black loading frame, so loading-state and motion evidence remain distinct. Center the approved icon optically around the S body (translation +32 right, −20 up from the prior export), preserving its paths and size.

Follow-up verification: independent signed onboarding, mixed-use lifecycle, and repeated light/dark tab checks passed; root inspected onboarding and sampled final tab-recording frames. Focused loading checks passed 17/17; integrated units passed 2,163 with 14 skips and zero failures. Controlled delayed optional discovery/cancellation is covered by units; actual tab re-entry is covered by Simulator. No physical-device FPS, camera scanning, exhaustive transient elimination, or working group-chat claim is implied. Failed/interrupted harness attempts remain retained with their causes. Group execution still requires the separately approved disposable-profile fixture.
