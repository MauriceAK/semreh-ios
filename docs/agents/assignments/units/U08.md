# U08: Verify bird/header, tab outline, and light/dark hit targets

Parent: #15. Seed ID: `U08-p10-header-acceptance`. Status: **CHECKLIST UNDER M04; NOT A SEPARATE WORKER**. Prepared only; no launch. Owner: Muse. Cloud model preference: Luna; native verification: Sol medium. Astra handles any resulting UI design decision.

## Owning task

[M04](../M04.md) owns execution, permitted writes, model, continuation and repair. Earlier preparation-only directions below are historical context and do not create an additional agent assignment. Product acceptance and device/authority gates remain binding.

## Outcome and scope

Use the assigned owning track; do not launch a duplicate investigation. Existing brief: Muse M01–M05.

Source baseline is master commit `0dd782fdf3be99013444b72bc2448c7d2e01a1dc`. Read `docs/agents/pstack/README.md` and `worker-playbooks.md` from PR #16. Runtime/implementation dispatch must pin its then-current integrated candidate explicitly; historical seed SHAs below are retained for traceability and require updating in the dispatch, not silent reuse.

Allowed preparation writes: this task's issue report only. No product files, tests, project membership, shared plan, personal state, services, or simulator writes during cloud preparation. This avoids collisions with C01–C03, Muse and existing PRs. Runtime execution belongs to the single native lane.

Potential implementation scope from seed: not yet established; select the smallest exact file set from a reproduced failure before granting write ownership. These are candidate areas, not permission to rewrite them.

## Acceptance criteria

- Inspect real light and dark screens: bird is larger/lower/centered, name overlaps the glass treatment as intended, and no dropdown/white logo tile appears.
- Outlined bird tab and transparent S hit targets are reachable without center drift or a hard header/content panel.
- Keep experimental bird-to-S morph deferred unless separately authorized.

## Dependencies and release conditions

Seed dependencies: No seed dependency. Shared responsibilities: G-native-serial-evidence, G-session-list.

Read-only preparation may run before these gates; native acceptance and code mutation must satisfy them. For code: obtain a concrete failing scenario or approved missing behavior, reconcile local uncommitted work, pin the base, register exact writable files and exclude active conflicts, then dispatch Luna unless complexity warrants Terra/Sol. UI design uses Astra. Do not invent a repair to fill capacity.

For native acceptance: pin the current SHA and approved disposable fixture; honor one simulator owner, signed builds, preserved app state and disabled diagnostic collection. Device/relay/fixture authority stays blocked where named in the criteria. Source review and green CI are not runtime evidence.

## Checks and report

Preparation: `git rev-parse HEAD`, `git status --porcelain`, relevant source/test searches and existing recipe inspection. Do not claim unexecuted tests. Return source citations, exact proposed XCTest suite/test identifiers from the checkout, reproduction inputs, expected assertions, boundary cases, and unresolved native checks. If a test already covers the criterion, cite it rather than duplicating it.

Implementation follow-up: focused available tests, macOS PR CI, then native verification of this criterion set. Worker repairs red CI; verifier failures return with exact SHA/evidence to the recorded worker or a new bounded correction task. Coordinator alone reviews and integrates.

Timebox: 30 minutes for cloud preparation. Return PASS/ISSUES/BLOCKED, actual SHA, commands, evidence or limitations, and next repair proposal. No merge, rebase, force-push, arbitrary provider access, personal Hermes/Tailscale changes, assertion weakening, or speculative feature expansion. New head commits require fresh verification.
