# M01: Settings clarity and supported controls

Issue #20; parent #15. Covers U09. Status: ready, awaiting Maurice's batch greenlight. Model: Muse provider model, recorded.

## Outcome

Supported model/reasoning/context/theme/accent controls are reachable and clear; context uses canonical usage; useless controls do not claim unsupported behavior; scoped settings preserve sibling isolation. Fix demonstrated gaps within the existing design; new visual design routes to Astra.

Own this bounded engineering task through diagnosis, confirmed repair, focused tests, PR CI, and verifier feedback. Do not stop after a planning report when an in-scope correction is justified. If existing behavior already satisfies the criteria, return evidence for native verification rather than inventing code. Verification-only subcriteria remain native work, not mandatory implementation.

## Source and ownership

Start from the exact master SHA supplied at dispatch. The previous 0dd782f baseline is historical; coordinator rechecks current source and open PRs first. Before code dispatch the coordinator reconciles relevant local dirty changes and includes accepted current context. Do not overwrite or assume access to unpublished Mac work.

Exclusive candidate production write scope:
```text
HermesMobile/Features/Settings/ (EXCLUDING StreamingLabView.swift and StreamingLabReplay.swift)
```
Test scope:
```text
Existing settings-specific tests only; request shared profile/model test ownership before changing it
```
Read dependencies freely within the repo. Shared project.pbxproj, networking contracts, AppShell and files assigned to another owner are excluded unless coordinator explicitly transfers ownership. New test files require target discovery/project membership to be resolved before creating them. Record the actual subset touched in the PR. Keep one branch `work/m01-semreh` per owner and one coherent change per PR. Dependent fixes proceed serially within this owner; unrelated owners continue.

## Method and acceptance

Read AGENTS.md and `docs/agents/pstack/README.md`, `worker-playbooks.md`, and the linked `units/Uxx.md` criteria for this coverage. This owner brief supersedes those files' earlier report-only dispatch directions, not their product acceptance criteria. Poteto's bug-fix/refactoring/performance route is chosen by the actual change.

1. Inspect implementation, existing tests and applicable evidence. Reproduce a bug with a focused regression when available. For native-only symptoms, request the exact verifier reproduction through the coordinator while completing useful source/test work.
2. Make the smallest confirmed in-scope correction. No speculative rewrite. Escalate a genuinely cross-boundary fix with the specific required paths; do not wait on the human for routine implementation choices already authorized by the task.
3. Run supported focused checks. Open a linked PR, record head SHA and actual provider task identity, and own macOS CI failures until green or a concrete blocker.
4. Supply the original scenario, expected result, test output and runtime limitations for independent Sol-medium native verification. Maintain ownership through returned findings. A changed commit requires renewed verification.
5. Finish with accepted evidence and a reviewable PR, or a demonstrated no-change result/precise blocker. Only the root coordinator integrates. Worker PASS is not a merge verdict.

Native evidence uses one signed warm simulator and approved disposable fixture. Appearance needs named-state screenshots, motion/lifecycle needs recordings, performance needs measured traces and environment. No simulator claim substitutes for physical-device acceptance. No product tests may use personal Hermes services or state.

## Continuation and bounds

First progress checkpoint within 30 minutes; initial execution budget 90 minutes. Report concrete branch/PR/check progress or a causal blocker. Two failed repair attempts require coordinator escalation rather than repeating the same hypothesis. A provider that cannot resume an existing task gets a replacement owner with the same issue, branch, findings and constraints. Do not pretend a PR mention guarantees automatic resumption.

Never merge, force-push, rebase without an explicit integration assignment, weaken tests, expose credentials, alter personal services/routes, or add unrelated features. Model escalation is Luna → Terra/Sol for hard implementation; Astra handles user-facing design. Actual provider support must be checked at launch.

Report issue/branch/head/PR, changed paths, criteria satisfied, exact checks, artifacts, limits and remaining repair. Update this issue at work/CI/verification/blocked transitions. The coordinator owns shared assignment state.
