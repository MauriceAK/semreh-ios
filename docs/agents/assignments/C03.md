# C03: Performance measurement coverage investigation

Parent backlog: #15. Covers U13/U14/U17/U18/U19/U21.

Status: ready; prepared only, no worker dispatched. Maurice's greenlight is required for this batch.

Owner lane: Codex. Model preference: Luna. Read-only investigation, not product implementation or acceptance.

## Goal and context

Determine which existing measurements actually measure frame timing/hitches versus callback intervals or test waits. Inventory realistic transcript fixtures and coverage for Markdown, code, tools, media, multi-chat switching, momentum jump, send, and background return. Recommend only missing measurable gates with exact command candidates and feasibility limits. No promised zero lag or physical FPS from source review.

Pinned source baseline: `0dd782fdf3be99013444b72bc2448c7d2e01a1dc` on MauriceAK/semreh-ios master. Record actual checkout SHA; report drift before widening scope. Existing PRs #10/#12/#13/#14 have overlapping refactors. PR #16 supplies portable PStack guidance; read its `docs/agents/pstack/README.md` and worker/orchestration playbooks from branch `docs/portable-pstack-workers` until merged. Do not silently change the product baseline to that documentation branch.

## Scope and output

Inspect HermesMobile/Features/Chat/ChatFrameCallbackTimingProbe.swift; StreamingLab files; HermesMobileUITests/LongChatScrollUITests.swift; related performance tests. Read dependencies only to establish the assigned behavior. Write no app code, tests, project files, or shared tracking files. Return one report in this issue with source path/symbol references, findings ranked by impact, relevant existing tests, concrete next repair scopes, and explicit runtime unknowns. No PR is needed for this investigation. Reports from different workers have separate issues; shared source reads are allowed.

## Acceptance and verification

- Every finding cites the pinned source and distinguishes observed source facts, hypotheses, and unexecuted native checks.
- Check existing tests and pending PR scope before proposing duplicate work.
- Each proposed repair has a failing scenario, smallest owned file set, regression check, and native acceptance steps.
- If no concrete gap is found, report that result; do not manufacture changes.
- Use `git rev-parse HEAD`, `git status --porcelain`, source searches, and existing relevant test definitions. Run only supported read-only/focused checks and report exact commands and outcomes. Linux cannot provide iOS runtime evidence.

Timebox: 30 minutes. Return partial findings and BLOCKED/ISSUES on expiry; do not loop indefinitely.

## Standing constraints

Coordinator owns assignments and merges. Preserve existing work. No rebase, force-push, branch mutation, personal Hermes state/services/credentials, Tailscale changes, simulator use, or provider deployment. No weakening tests or inferred API contracts. Luna is default for bounded implementation; Terra/Sol handle hard repairs; Astra is reserved for UI design; Sol medium performs native verification. Provider support must be checked before claiming a requested model. New code work requires the coordinator's bounded follow-up assignment. Native reviewer acceptance remains separate from this investigation.
